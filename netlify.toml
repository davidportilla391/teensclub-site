<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Carta de Bebidas · TeensClub®</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;1,700&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    :root {
      --navy:    #0a1330;
      --navy2:   #0d1840;
      --white:   #f5f4f0;
      --offwhite:#c8c5bc;
      --border:  rgba(255,255,255,0.09);
      --glass:   rgba(255,255,255,0.04);
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    html { scroll-behavior: smooth; }

    body {
      background: var(--navy);
      color: var(--white);
      font-family: 'DM Sans', sans-serif;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* ── PARTICLES CANVAS ── */
    #particles {
      position: fixed;
      inset: 0;
      z-index: 0;
      pointer-events: none;
      opacity: 0.5;
    }

    /* ── GLOW ── */
    .glow-top {
      position: fixed;
      top: -200px;
      left: 50%;
      transform: translateX(-50%);
      width: 600px;
      height: 500px;
      background: radial-gradient(ellipse, rgba(80,110,220,0.22) 0%, transparent 70%);
      pointer-events: none;
      z-index: 0;
      transition: transform 0.1s ease;
    }

    .container {
      position: relative;
      z-index: 1;
      max-width: 480px;
      margin: 0 auto;
      padding: 0 0 80px;
    }

    /* ── HEADER ── */
    .header {
      text-align: center;
      padding: 60px 24px 40px;
      position: relative;
    }

    .logo {
      font-weight: 500;
      font-size: 12px;
      letter-spacing: 0.38em;
      text-transform: uppercase;
      color: var(--offwhite);
      margin-bottom: 16px;
      opacity: 0;
      animation: fadeDown 0.8s ease 0.1s forwards;
    }

    .title {
      font-family: 'Playfair Display', serif;
      font-style: italic;
      font-size: 42px;
      color: var(--white);
      line-height: 1.1;
      opacity: 0;
      animation: fadeDown 0.8s ease 0.25s forwards;
    }

    .title span { font-style: normal; }

    .subtitle {
      font-size: 11px;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: rgba(200,197,188,0.4);
      margin-top: 12px;
      opacity: 0;
      animation: fadeDown 0.8s ease 0.4s forwards;
    }

    .divider-line {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 12px;
      margin: 28px auto 0;
      opacity: 0;
      animation: fadeDown 0.8s ease 0.55s forwards;
    }

    .divider-line::before,
    .divider-line::after {
      content: '';
      width: 40px;
      height: 1px;
      background: rgba(255,255,255,0.15);
    }

    .divider-dot {
      width: 4px;
      height: 4px;
      border-radius: 50%;
      background: rgba(255,255,255,0.3);
    }

    @keyframes fadeDown {
      from { opacity: 0; transform: translateY(-12px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    /* ── SECTION ── */
    .section {
      padding: 0 18px;
      margin-top: 42px;
    }

    .section-label {
      font-size: 10px;
      letter-spacing: 0.25em;
      text-transform: uppercase;
      color: rgba(200,197,188,0.45);
      margin-bottom: 16px;
      display: flex;
      align-items: center;
      gap: 12px;
      opacity: 0;
      transform: translateX(-20px);
      transition: opacity 0.6s ease, transform 0.6s ease;
    }

    .section-label.visible {
      opacity: 1;
      transform: translateX(0);
    }

    .section-label::before,
    .section-label::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--border);
    }

    /* ── ITEM ── */
    .item {
      background: var(--glass);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 16px 20px;
      margin-bottom: 10px;
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 14px;
      cursor: default;
      opacity: 0;
      transform: translateY(24px);
      transition:
        opacity 0.55s ease,
        transform 0.55s ease,
        background 0.25s ease,
        border-color 0.25s ease,
        box-shadow 0.25s ease;
      will-change: transform, opacity;
    }

    .item.visible {
      opacity: 1;
      transform: translateY(0);
    }

    .item:hover {
      background: rgba(255,255,255,0.07);
      border-color: rgba(255,255,255,0.16);
      box-shadow: 0 8px 32px rgba(0,0,0,0.3);
      transform: translateY(-2px);
    }

    .item:active {
      transform: scale(0.98);
    }

    .item-left { flex: 1; }

    .item-name {
      font-size: 15px;
      font-weight: 500;
      color: var(--white);
      line-height: 1.3;
    }

    .item-desc {
      font-size: 12px;
      color: rgba(200,197,188,0.42);
      margin-top: 5px;
      line-height: 1.55;
      font-weight: 300;
    }

    .item-price {
      font-family: 'Playfair Display', serif;
      font-size: 20px;
      color: var(--white);
      white-space: nowrap;
      padding-top: 1px;
      transition: transform 0.2s ease;
    }

    .item:hover .item-price {
      transform: scale(1.05);
    }

    .badge {
      display: inline-block;
      font-size: 9px;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      padding: 2px 8px;
      border-radius: 20px;
      margin-left: 7px;
      vertical-align: middle;
      font-weight: 500;
      font-family: 'DM Sans', sans-serif;
      background: rgba(255,255,255,0.07);
      color: var(--offwhite);
      border: 1px solid rgba(255,255,255,0.11);
    }

    /* ── BOTELLA ESPECIAL ── */
    .item-special {
      background: rgba(255,255,255,0.06);
      border-color: rgba(255,255,255,0.16);
      position: relative;
      overflow: hidden;
    }

    .item-special::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(ellipse 40% 40% at 60% 60%, rgba(255,255,255,0.04) 0%, transparent 70%);
      pointer-events: none;
    }

    .item-special .item-name {
      font-family: 'Playfair Display', serif;
      font-style: italic;
      font-size: 16px;
    }

    .item-special .item-price { font-size: 22px; }

    /* shimmer on special */
    .item-special::after {
      content: '';
      position: absolute;
      top: 0; left: -100%;
      width: 60%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.05), transparent);
      animation: shimmer 3s ease-in-out infinite;
    }

    @keyframes shimmer {
      0%   { left: -100%; }
      50%  { left: 150%; }
      100% { left: 150%; }
    }

    /* ── FOOTER ── */
    .footer {
      margin: 44px 18px 0;
      padding: 22px;
      border: 1px solid var(--border);
      border-radius: 16px;
      text-align: center;
      background: var(--glass);
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.6s ease, transform 0.6s ease;
    }

    .footer.visible {
      opacity: 1;
      transform: translateY(0);
    }

    .footer p {
      font-size: 12px;
      color: rgba(200,197,188,0.38);
      line-height: 1.9;
      font-weight: 300;
    }

    .footer strong {
      color: rgba(200,197,188,0.62);
      font-weight: 500;
    }

    .stamp {
      display: inline-block;
      margin-top: 14px;
      font-size: 10px;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: rgba(200,197,188,0.3);
      border: 1px solid rgba(200,197,188,0.13);
      padding: 4px 16px;
      border-radius: 20px;
    }

    /* ── STAGGER DELAYS ── */
    .item:nth-child(1) { transition-delay: 0s; }
    .item:nth-child(2) { transition-delay: 0.07s; }
    .item:nth-child(3) { transition-delay: 0.14s; }
    .item:nth-child(4) { transition-delay: 0.21s; }
    .item:nth-child(5) { transition-delay: 0.28s; }
  </style>
</head>
<body>

<canvas id="particles"></canvas>
<div class="glow-top" id="glow"></div>

<div class="container">

  <div class="header">
    <div class="logo">TeensClub®</div>
    <div class="title"><span>Carta de</span> bebidas.</div>
    <div class="subtitle">100% sin alcohol · Bar</div>
    <div class="divider-line"><div class="divider-dot"></div></div>
  </div>

  <!-- MOCKTAILS -->
  <div class="section">
    <div class="section-label">Mocktails de autor</div>

    <div class="item">
      <div class="item-left">
        <div class="item-name">Tropic Rush <span class="badge">Popular</span></div>
        <div class="item-desc">Naranja · maracuyá · grenadine · agua mineral</div>
      </div>
      <div class="item-price">$80</div>
    </div>

    <div class="item">
      <div class="item-left">
        <div class="item-name">Green Mint</div>
        <div class="item-desc">Menta fresca · limón · soda · base de té verde</div>
      </div>
      <div class="item-price">$80</div>
    </div>

    <div class="item">
      <div class="item-left">
        <div class="item-name">Pink Smash</div>
        <div class="item-desc">Sandía natural · limón amarillo · jengibre · hielo triturado</div>
      </div>
      <div class="item-price">$80</div>
    </div>

    <div class="item">
      <div class="item-left">
        <div class="item-name">Electric Blue</div>
        <div class="item-desc">Curaçao azul sin alcohol · piña · lima · soda</div>
      </div>
      <div class="item-price">$80</div>
    </div>

    <div class="item">
      <div class="item-left">
        <div class="item-name">Berry Lemonade</div>
        <div class="item-desc">Fresa natural · limón · jarabe · menta</div>
      </div>
      <div class="item-price">$80</div>
    </div>
  </div>

  <!-- CHAMPAÑA -->
  <div class="section">
    <div class="section-label">Champaña sin alcohol</div>

    <div class="item">
      <div class="item-left">
        <div class="item-name">Copa Blanca</div>
        <div class="item-desc">Espumoso de uva blanca · servido en copa fría</div>
      </div>
      <div class="item-price">$99</div>
    </div>

    <div class="item">
      <div class="item-left">
        <div class="item-name">Copa Rosada</div>
        <div class="item-desc">Espumoso de uva rosada · notas de frambuesa · copa fría</div>
      </div>
      <div class="item-price">$99</div>
    </div>

    <div class="item item-special">
      <div class="item-left">
        <div class="item-name">Champaña TC <span class="badge">VIP</span></div>
        <div class="item-desc">Botella 750ml · 6 copas · bengalas · presentación especial</div>
      </div>
      <div class="item-price">$380</div>
    </div>
  </div>

  <!-- REFRESCOS -->
  <div class="section">
    <div class="section-label">Refrescos & agua</div>

    <div class="item">
      <div class="item-left">
        <div class="item-name">Refresco</div>
        <div class="item-desc">Coca-Cola · Sprite · Manzanita · Peach Tea · por vaso</div>
      </div>
      <div class="item-price">$50</div>
    </div>

    <div class="item">
      <div class="item-left">
        <div class="item-name">Agua natural o mineral</div>
        <div class="item-desc">500ml · con o sin gas</div>
      </div>
      <div class="item-price">$30</div>
    </div>
  </div>

  <!-- FOOTER -->
  <div class="footer">
    <p>
      <strong>100% sin alcohol</strong> · El bar acepta efectivo y tarjeta<br>
      Precios en MXN con IVA incluido<br>
      No se permite ingreso de bebidas externas
    </p>
    <div class="stamp">Evento sin alcohol</div>
  </div>

</div>

<script>
  // ── PARTICLES ──
  const canvas = document.getElementById('particles');
  const ctx = canvas.getContext('2d');
  let particles = [];

  function resize() {
    canvas.width  = window.innerWidth;
    canvas.height = window.innerHeight;
  }
  resize();
  window.addEventListener('resize', resize);

  function createParticle() {
    return {
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      r: Math.random() * 1.2 + 0.3,
      dx: (Math.random() - 0.5) * 0.3,
      dy: -Math.random() * 0.4 - 0.1,
      alpha: Math.random() * 0.4 + 0.1,
      life: 0,
      maxLife: Math.random() * 300 + 200
    };
  }

  for (let i = 0; i < 60; i++) particles.push(createParticle());

  function animateParticles() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    particles.forEach((p, i) => {
      p.x += p.dx;
      p.y += p.dy;
      p.life++;
      const fade = p.life < 40 ? p.life / 40 : p.life > p.maxLife - 40 ? (p.maxLife - p.life) / 40 : 1;
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(255,255,255,${p.alpha * fade})`;
      ctx.fill();
      if (p.life >= p.maxLife || p.y < -10) particles[i] = createParticle();
    });
    requestAnimationFrame(animateParticles);
  }
  animateParticles();

  // ── PARALLAX GLOW ON SCROLL ──
  const glow = document.getElementById('glow');
  window.addEventListener('scroll', () => {
    const y = window.scrollY;
    glow.style.transform = `translateX(-50%) translateY(${y * 0.3}px)`;
  });

  // ── SCROLL REVEAL ──
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
      }
    });
  }, { threshold: 0.12 });

  document.querySelectorAll('.item, .section-label, .footer').forEach(el => {
    observer.observe(el);
  });

  // ── RIPPLE ON ITEM TAP ──
  document.querySelectorAll('.item').forEach(item => {
    item.addEventListener('pointerdown', function(e) {
      const rect = this.getBoundingClientRect();
      const ripple = document.createElement('span');
      const size = Math.max(rect.width, rect.height) * 2;
      ripple.style.cssText = `
        position:absolute; border-radius:50%;
        width:${size}px; height:${size}px;
        left:${e.clientX - rect.left - size/2}px;
        top:${e.clientY - rect.top - size/2}px;
        background:rgba(255,255,255,0.06);
        transform:scale(0); animation:ripple 0.5s ease forwards;
        pointer-events:none;
      `;
      this.style.position = 'relative';
      this.style.overflow = 'hidden';
      this.appendChild(ripple);
      setTimeout(() => ripple.remove(), 600);
    });
  });

  const style = document.createElement('style');
  style.textContent = `@keyframes ripple { to { transform: scale(1); opacity: 0; } }`;
  document.head.appendChild(style);
</script>

</body>
</html>
